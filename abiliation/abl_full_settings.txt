================================================================
ABLATION RUN SETTINGS — abl_full (row 9)  ★ FULL MODEL
================================================================
Study    : VnPollenBee ablation (abiliation/study_specifications.md)
Purpose  : Full model — all four components ON together. The
           candidate final model (§9) and the top row of the
           contribution story. Jumped here ahead of the leave-one-out
           rows 5-8 so the longest, headline run starts early.
Branch   : run/abl_full
Commit   : PENDING
Date     : 2026-07-28
Seed     : 42

----------------------------------------------------------------
COMPONENTS (protocol §2/§3) — ALL ON
----------------------------------------------------------------
CBAM      : ON    (POLLENBEES_ATTN=cbam_botnet — CBAM on P3/P4/P5)
BoT       : ON    (POLLENBEES_ATTN=cbam_botnet — P5 C2f -> BoTNet MHSA)
SAF-SIoU  : ON    (box_loss_type: saf_siou)
VFL-w     : ON    (cls_loss_type: vfl, cls_pos_weight: [1.0, 2.0])

Config delta vs abl_baseline — FOUR factors across TWO surfaces:
  env var        : POLLENBEES_ATTN none -> cbam_botnet   (CBAM + BoT)
  utils/args.yaml: box_loss_type ciou -> saf_siou        (SAF-SIoU)
  utils/args.yaml: cls_loss_type bce -> vfl              (VFL)
  utils/args.yaml: cls_pos_weight [1,1] -> [1,2]         (minority weight)
This is the ONE row that legitimately changes everything at once —
it is the combined model, not a one-factor ablation.

----------------------------------------------------------------
ARCHITECTURE
----------------------------------------------------------------
Model            : YOLOv8m (custom re-implementation, MyYolo)
Classes          : 2 (nonpollenbee=0, pollenbee=1)
Pretrained       : none — trained from scratch
Input resolution : 1280 x 1280 (direct resize, no letterboxing)
Params           : 22.71M — LIGHTER than the 25.86M stock baseline.
                   BoT's MHSA replaces the heavier P5 C2f, outweighing
                   what CBAM adds. "More capable AND smaller" — a real
                   contribution bullet. (verified via CPU build @1280)

De-risk (local): full stack verified — cbam_botnet builds+forwards at
1280 (rel-pos assert OK); combined SAF-SIoU + weighted-VFL loss runs
forward+backward, gradients flow, WIoU running-mean updates, pos_weight
tensor [1.0, 2.0]. Checked on CPU.

----------------------------------------------------------------
TRAINING (invariants — identical for every ablation row)
----------------------------------------------------------------
Epochs           : 100 fixed
Early stopping   : OFF (POLLENBEES_PATIENCE=0)
Batch size       : 4
Optimizer        : SGD, Nesterov, momentum 0.937, weight decay 5e-4
LR schedule      : 0.01 -> 1e-4, cosine annealing over 100 epochs
Gradient clip    : max-norm 10
Loss gains       : box 7.5 / cls 0.5 / dfl 1.5

Augmentation (train split only, scale-preserving):
  HSV jitter     : h 0.015 / s 0.7 / v 0.4
  Horizontal flip: p 0.5
  Vertical flip  : p 0.0
  Mosaic/affine  : OFF for all rows (object-size statistics must stay
                   matched to evaluation)
Val/test         : never augmented

----------------------------------------------------------------
MODEL SELECTION + EVALUATION
----------------------------------------------------------------
best.pt selected : pollen fitness = 0.1*AP@0.5 + 0.9*AP@0.5:0.95,
                   pollenbee class only, on validation every epoch
mAP              : torchmetrics, IoU 0.50:0.95, NMS conf 0.001 / IoU 0.70
P/R/F1 stats     : conf 0.5, IoU 0.5, greedy one-to-one matching
Test set         : ★ For the FULL / final model, the test row IS the
                   reportable headline (§9: test evaluated once, on the
                   final model). Rows 0-4 ranked on val with test sealed;
                   this row is where test becomes the number you publish.
                   (Confirm "final model" after leave-one-out rows 5-8 —
                   if a component is redundant the final model may drop it.)
Outputs          : CSV runs/results/abl_full_<runid>.csv
                   checkpoints runs/abl_full_<runid>/

----------------------------------------------------------------
LAUNCH (Colab)
----------------------------------------------------------------
git clone -b run/abl_full <repo> ; verify commit above

POLLENBEES_DATA       = /content/pollenbees_dataset
POLLENBEES_OUT        = /content/drive/MyDrive/Alireza/Bee
POLLENBEES_WORKERS    = 2
POLLENBEES_PIN        = 1
POLLENBEES_ATTN       = cbam_botnet
POLLENBEES_MODEL_NAME = abl_full
POLLENBEES_PATIENCE   = 0
python phase1_pipeline_v2.py

Clone sanity — this row must pass ALL FOUR (env + 3 config lines):
  POLLENBEES_ATTN env               -> cbam_botnet
  grep "^box_loss_type" args.yaml   -> saf_siou
  grep "^cls_loss_type" args.yaml   -> vfl
  grep -A2 "^cls_pos_weight" ...    -> 1.0 then 2.0
Log must show "Attention mode: cbam_botnet" and "Parameters: 22.71M".

----------------------------------------------------------------
RESULTS (fill in after the run)
----------------------------------------------------------------
GPU / runtime         : ................
Total / best epoch    : ................
run_id                : ................
Pollen AP@0.5:0.95    : ................   (VAL for ranking; TEST is reportable)
Pollen AP@0.5         : ................
Pollen P / R / F1     : ................
Pollen TP/FP/FN       : ................   (MR, FAR)
Overall mAP@0.5       : ................
Overall mAP@0.5:0.95  : ................
Non-pollen F1 (guard) : ................   (watch — vflw dipped this)
Notes                 : ................

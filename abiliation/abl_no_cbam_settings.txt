================================================================
ABLATION RUN SETTINGS — abl_no_cbam (row 5)  [leave-one-out]
================================================================
Study    : VnPollenBee ablation (abiliation/study_specifications.md)
Purpose  : Full model MINUS CBAM. Leave-one-out row — isolates CBAM's
           MARGINAL contribution on top of BoT + SAF-SIoU + VFL-w.
           Leave-one-in (row 1, abl_cbam) showed CBAM alone was +0.002
           AP over baseline (within noise); this row asks the harder
           question the leave-one-in cannot: does CBAM still add
           anything once BoT/SAF/VFL are already present? If AP is
           unchanged vs abl_full, CBAM is redundant in the full stack
           and the final model can drop it.
Branch   : run/abl_no_cbam  (branched from run/abl_full — same code,
           one factor removed)
Commit   : 06ce137
Date     : 2026-07-31
Seed     : 42

----------------------------------------------------------------
COMPONENTS (protocol §2/§3) — CBAM OFF, other three ON
----------------------------------------------------------------
CBAM      : OFF   (POLLENBEES_ATTN=botnet — modules removed entirely)
BoT       : ON    (POLLENBEES_ATTN=botnet — P5 C2f -> BoTNet MHSA)
SAF-SIoU  : ON    (box_loss_type: saf_siou)
VFL-w     : ON    (cls_loss_type: vfl, cls_pos_weight: [1.0, 2.0])

Config delta vs abl_full (row 9) — EXACTLY ONE factor:
  env var : POLLENBEES_ATTN cbam_botnet -> botnet   (drop CBAM)
  utils/args.yaml : UNCHANGED (saf_siou + vfl + [1,2] all identical)
This is a valid one-factor leave-one-out: only CBAM changes.

Architecture note: this is the SAME network as abl_bot (row 2,
POLLENBEES_ATTN=botnet). The only difference from abl_bot is the LOSS
stack — abl_bot used stock CIoU+BCE, this row uses SAF-SIoU + weighted
VFL. So abl_no_cbam vs abl_bot also reads as "full losses on the BoT
backbone", but its designated role in the matrix is Full - CBAM.

----------------------------------------------------------------
ARCHITECTURE
----------------------------------------------------------------
Model            : YOLOv8m (custom re-implementation, MyYolo)
Classes          : 2 (nonpollenbee=0, pollenbee=1)
Pretrained       : none — trained from scratch
Input resolution : 1280 x 1280 (direct resize, no letterboxing)
Params           : ~22.6M — same backbone as abl_bot (BoT, no CBAM).
                   Lighter than the 25.86M stock baseline (BoT's MHSA
                   replaces the heavier P5 C2f); CBAM's small add is
                   absent here. Confirm exact count in the run log
                   ("Parameters:" line) — must match abl_bot, NOT
                   abl_full's 22.71M.

----------------------------------------------------------------
TRAINING (invariants — identical for every ablation row)
----------------------------------------------------------------
Epochs           : 100 fixed
Early stopping   : OFF (POLLENBEES_PATIENCE=0)
Batch size       : 4
Optimizer        : SGD, Nesterov, momentum 0.937, weight decay 5e-4
LR schedule      : 0.01 -> 1e-4, cosine annealing over 100 epochs
Gradient clip    : max-norm 10
Loss gains       : box 7.5 / cls 0.5 / dfl 1.5

Augmentation (train split only, scale-preserving):
  HSV jitter     : h 0.015 / s 0.7 / v 0.4
  Horizontal flip: p 0.5
  Vertical flip  : p 0.0
  Mosaic/affine  : OFF for all rows (object-size statistics must stay
                   matched to evaluation)
Val/test         : never augmented

----------------------------------------------------------------
MODEL SELECTION + EVALUATION
----------------------------------------------------------------
best.pt selected : pollen fitness = 0.1*AP@0.5 + 0.9*AP@0.5:0.95,
                   pollenbee class only, on validation every epoch
mAP              : torchmetrics, IoU 0.50:0.95, NMS conf 0.001 / IoU 0.70
P/R/F1 stats     : conf 0.5, IoU 0.5, greedy one-to-one matching
Ranking metric   : VAL pollen AP@0.5:0.95 (§8 — test stays sealed for
                   leave-one-out rows; only the final model touches test)
Outputs          : CSV runs/results/abl_no_cbam_<runid>.csv
                   checkpoints runs/abl_no_cbam_<runid>/

----------------------------------------------------------------
LAUNCH (Colab)
----------------------------------------------------------------
git clone -b run/abl_no_cbam <repo> ; verify commit above

POLLENBEES_DATA       = /content/pollenbees_dataset
POLLENBEES_OUT        = /content/drive/MyDrive/Alireza/Bee
POLLENBEES_WORKERS    = 2
POLLENBEES_PIN        = 1
POLLENBEES_ATTN       = botnet
POLLENBEES_MODEL_NAME = abl_no_cbam
POLLENBEES_PATIENCE   = 0
python phase1_pipeline_v2.py

Clone sanity — this row must pass ALL FOUR:
  POLLENBEES_ATTN env               -> botnet   (NOT cbam_botnet)
  grep "^box_loss_type" args.yaml   -> saf_siou
  grep "^cls_loss_type" args.yaml   -> vfl
  grep -A2 "^cls_pos_weight" ...    -> 1.0 then 2.0
Log must show "Attention mode: botnet" (no CBAM) and the abl_bot param
count (~22.6M, NOT 22.71M).

----------------------------------------------------------------
RESULTS (fill in after the run)
----------------------------------------------------------------
GPU / runtime         : ................
Total / best epoch    : ................
run_id                : ................
Pollen AP@0.5:0.95    : ................   (VAL — ranking metric, §8)
Pollen AP@0.5         : ................
Pollen P / R / F1     : ................
Pollen TP/FP/FN       : ................   (MR, FAR)
Overall mAP@0.5       : ................
Overall mAP@0.5:0.95  : ................
Non-pollen F1 (guard) : ................
Notes                 : compare AP vs abl_full — if within seed noise,
                        CBAM is redundant in the full stack.

================================================================
ABLATION RUN SETTINGS — abl_saf (row 3)
================================================================
Study    : VnPollenBee ablation (abiliation/study_specifications.md)
Purpose  : SAF-SIoU alone — measure the isolated effect of the
           composite SAF-SIoU box loss (SIoU + NWD gate + WIoU-v3
           focusing) against the stock CIoU baseline (row 0).
Branch   : run/abl_saf
Commit   : 2503504
Date     : 2026-07-27
Seed     : 42

----------------------------------------------------------------
COMPONENTS (protocol §2/§3)
----------------------------------------------------------------
CBAM      : OFF   (POLLENBEES_ATTN=none)
BoT       : OFF   (POLLENBEES_ATTN=none — final stage keeps C2f)
SAF-SIoU  : ON    (utils/args.yaml box_loss_type: saf_siou —
                   SIoU shape cost + NWD size gate + WIoU-v3 dynamic
                   focusing, replaces CIoU on the box regression term)
VFL-w     : OFF   (cls_loss_type: bce — stock BCE classification loss)

Config delta vs abl_baseline: box_loss_type ciou -> saf_siou.
THIS IS A utils/args.yaml EDIT, not an env var — the first ablation
row that carries a committed config diff (rows 0-2 were env-var only).
attn stays none, cls_loss_type stays bce, cls_pos_weight stays [1,1].

SAF hyperparameters (already in args.yaml, active only under saf_siou;
tuned for 1280 input-scale pixels — do NOT change per §8.1):
  saf_theta 4  |  saf_C 12.8  |  saf_T 32.0  |
  saf_alpha_f 1.9  |  saf_delta 3.0  |  saf_momentum 0.999
  box_class_coupling: false (SAF not coupled to class weights)

----------------------------------------------------------------
ARCHITECTURE
----------------------------------------------------------------
Model            : YOLOv8m (custom re-implementation, MyYolo)
Classes          : 2 (nonpollenbee=0, pollenbee=1)
Pretrained       : none — trained from scratch
Input resolution : 1280 x 1280 (direct resize, no letterboxing)
Params           : 25.86M (stock — SAF is loss-only, no param change)

De-risk (local): SAF-SIoU loss verified end-to-end — forward+backward
run, gradients flow, WIoU running-mean buffer updates, stride_tensor
correctly threaded (util.py:859). cls path stays BCE. Checked on CPU.

----------------------------------------------------------------
TRAINING (invariants — identical for every ablation row)
----------------------------------------------------------------
Epochs           : 100 fixed
Early stopping   : OFF (POLLENBEES_PATIENCE=0)
Batch size       : 4
Optimizer        : SGD, Nesterov, momentum 0.937, weight decay 5e-4
LR schedule      : 0.01 -> 1e-4, cosine annealing over 100 epochs
Gradient clip    : max-norm 10
Loss gains       : box 7.5 / cls 0.5 / dfl 1.5

Augmentation (train split only, scale-preserving):
  HSV jitter     : h 0.015 / s 0.7 / v 0.4
  Horizontal flip: p 0.5
  Vertical flip  : p 0.0
  Mosaic/affine  : OFF for all rows (object-size statistics must stay
                   matched to evaluation)
Val/test         : never augmented

----------------------------------------------------------------
MODEL SELECTION + EVALUATION (invariants)
----------------------------------------------------------------
best.pt selected : pollen fitness = 0.1*AP@0.5 + 0.9*AP@0.5:0.95,
                   pollenbee class only, on validation every epoch
mAP              : torchmetrics, IoU 0.50:0.95, NMS conf 0.001 / IoU 0.70
P/R/F1 stats     : conf 0.5, IoU 0.5, greedy one-to-one matching
Test set         : evaluated once, with best.pt, at end of run
                   (sealed until final model per §8.2 — informational)
Outputs          : CSV runs/results/abl_saf_<runid>.csv
                   checkpoints runs/abl_saf_<runid>/

----------------------------------------------------------------
LAUNCH (Colab)
----------------------------------------------------------------
git clone -b run/abl_saf <repo> ; verify commit above

POLLENBEES_DATA       = /content/pollenbees_dataset
POLLENBEES_OUT        = /content/drive/MyDrive/Alireza/Bee
POLLENBEES_WORKERS    = 2
POLLENBEES_PIN        = 1
POLLENBEES_ATTN       = none
POLLENBEES_MODEL_NAME = abl_saf
POLLENBEES_PATIENCE   = 0
python phase1_pipeline_v2.py

First-few-lines sanity: log must show "Attention mode: none",
"Parameters: 25.86M", and the box loss must be SAF (confirm via the
args.yaml box_loss_type: saf_siou in the clone).

----------------------------------------------------------------
RESULTS (fill in after the run)
----------------------------------------------------------------
GPU / runtime         : (fill from Colab)
Total / best epoch    : 100 / 79
run_id                : 20260727_194110
Pollen AP@0.5:0.95    : val 0.6799 (ranking) | test 0.7163   (primary ranking metric, VAL)
Pollen AP@0.5         : val 0.9218 | test (see CSV)
Pollen P / R / F1     : val 0.9379 / 0.8860 / 0.9112 | test F1 0.9482
Pollen TP/FP/FN (val) : 272 / 18 / 35   (MR 0.114, FAR 0.062)
Overall mAP@0.5       : val 0.9546
Overall mAP@0.5:0.95  : val 0.7599
Non-pollen F1 (guard) : val 0.9838   (majority class not regressed)
Notes                 : Rank on VAL per §8. Val AP@0.5:0.95 0.6799 = +0.0079
                        over baseline — below the §7 noise floor (0.01-0.02),
                        single seed, so not yet significant. But the CLEANEST
                        profile of rows 1-3: recall 0.857->0.886 (catches more
                        pollen) with precision essentially held (0.939->0.938,
                        FAR 0.061->0.062), so MR drops 0.143->0.114. Contrast
                        BoT, which bought recall by spending precision. F1 0.911
                        > BoT's 0.896 even though BoT leads on AP (F1@0.5 vs AP
                        divergence again, §8.3 — rank on AP). Strongest test row
                        so far (AP 0.716, FP 5 / FN 8) but test sealed (§8.2).
